# 文件与流操作
## 1. 核心概念：C++ 的 “流” 与文件操作
流（stream）：可以理解为 “数据的流动通道”—— 输入流是数据从外部（文件 / 控制台）流向程序，输出流是数据从程序流向外部（文件 / 控制台）。

文件流：专门用于文件的流，核心包含 3 个类：

| 类名      | 作用                          |
|-----------|-------------------------------|
| ofstream  | 输出文件流，用于创建文件并向文件写入信息      |
| ifstream  | 输入文件流，用于从文件读取信息      |
| fstream   | 文件流，且同时具有 ofstream 和 ifstream 两种功能 |

- 要在 C++ 中进行文件处理，必须在 C++ 源代码文件中包含头文件 `<iostream> `和 `<fstream>`
### 2. 文件的打开与关闭
在从文件读取信息或者向文件写入信息之前，必须先打开文件。**ofstream**和 **fstream** 对象都可以用来打开文件进行写操作，如果只需要打开文件进行读操作，则使用 **ifstream** 对象。

| 模式标志      | 描述                          |
|-----------|-------------------------------|
| ios::app  | 追加模式。所有写入都追加到文件末尾     |
| ios::ate  | 文件打开后定位到文件末尾      |
| ios::in  | 打开文件用于读取 |
| ios::in  | 打开文件用于写入 |
| ios::trunc  | 如果该文件已经存在，其内容将在打开文件之前被截断，即把文件长度设为 0 |

- 模式可以结合使用，用`|`间隔
关闭用` void close(); `即可

### 3. 文件的写入与读取
- 其实可以类比cout和cin，只是把终端变成了文件。对象变成了**ofstream**和**ifstream**  （**fstream**）通用
具体使用情形可见`writing_test.cpp`
